apiVersion: apps/v1
kind: Deployment

metadata:
  name: alertx-deployment

spec:
  replicas: 1

  selector:
    matchLabels:
      app: alertx-label

  template:
    metadata:
      labels:
        app: alertx-label

    spec:
      containers:
        - name: alertx
          image: sunnykumar13/alertx-web

          env:
            - name: POSTGRES_DB
              value: "sos_db"

            - name: POSTGRES_USER
              value: "postgres"

            - name: POSTGRES_PASSWORD
              value: "postgres"

            - name: POSTGRES_HOST
              value: "db"

            - name: POSTGRES_PORT
              value: "5432"

            - name: ALLOWED_HOSTS
              value: "*"

          ports:
            - containerPort: 8000